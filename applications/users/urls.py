# Django Imports
from django.urls import path, include

# Local Imports
from . import views

app_name = 'users_app'

urlpatterns = [
    # path('', views.NewDepartamentoView.as_view(), name='new-departamento'),
]